<!-- DEPENDS_ON: index.md#project-conventions -->

# Auth rule

Derived from **just one** of index.md's concepts — `project-conventions` — so the DEPENDS_ON above is
**scoped** with `#project-conventions` (opt-in). A change to `naming-rules` alone won't flag this file.
The path resolves to a sibling that exists, and the concept is one `index.md` actually DEFINES.
