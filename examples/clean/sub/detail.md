<!-- DEPENDS_ON: ../auth.md -->

# Detail

A nested file whose DEPENDS_ON points one directory up — resolves relative to this file. Left **unscoped**
on purpose: a bare path = whole-file (the always-relevant default), the safety-net half beside
`auth.md`'s opt-in `#project-conventions` scope.
