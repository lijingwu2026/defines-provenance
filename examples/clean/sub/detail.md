<!-- DEPENDS_ON: ../auth.md -->

# Detail

A nested file whose DEPENDS_ON points one directory up — resolves relative to this file. Left **unscoped**
on purpose: a **broad consumer** keeps a bare path = whole-file (the always-safe default), the safety-net
half beside `auth.md`'s focused `#project-conventions` scope.
