<!-- DEPENDS_ON: rules/ghost.md -->

# Bad

This DEPENDS_ON points at `rules/ghost.md`, which does not exist — the checker must flag exactly this
one as 🔴 (a path containing a directory that does not resolve).
